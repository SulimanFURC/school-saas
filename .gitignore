node_modules/
dist/
*.log
.DS_Store

# Local env (never commit secrets)
server/.env
client/.env

# Uploaded tenant logos (local dev)
server/uploads/

# Cursor IDE — local AI rules & skills (keep on machine; not shared via remote)
.cursor/rules/
.cursor/skills/
